<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.practicals">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Practicals">

        <activity
            android:name=".ui_widget.TaskButtons"
            android:exported="true" >
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui_widget.Toast"
            android:exported="true" />

        <activity
            android:name=".ui_widget.RegistrationForm"
            android:exported="true"
            android:theme="@style/Theme.Practicals.NoActionBar"/>

        <activity
            android:name=".ui_widget.MainActivity"
            android:exported="true" />

    </application>
</manifest>